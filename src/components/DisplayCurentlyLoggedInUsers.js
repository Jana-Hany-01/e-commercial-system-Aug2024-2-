import axios from "../api/axios";
const DisplayCurentlyLoggedInUsers = () => {
  // fetching the users from the backend but the backend does not seem to be providing this

  return (
    <div>
      <h1> The Currently Logged in Users : </h1>
      {/* <ol>
    
</ol> */}
    </div>
  );
};
export default DisplayCurentlyLoggedInUsers;
